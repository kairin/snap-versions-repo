# Migration Log

This log tracks applications that have been uninstalled from `apt` and reinstalled via `snap`.

* **2025-11-02:** `shellcheck` was uninstalled from `apt` and reinstalled via `snap`.

* **2025-11-02:** Updated snap versions in `installed_snaps.md` and `snap_versions.md`.